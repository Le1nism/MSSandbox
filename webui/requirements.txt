# Web framework for the UI
flask==2.3.3

# HTTP client for making requests to microservices
requests==2.31.0

# Environment variables
python-dotenv==1.0.0

# System metrics (not strictly needed in UI, but keep consistent if we want local metrics later)
psutil==5.9.8